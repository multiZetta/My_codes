
// ПРИМЕР ЗАПИСИ КОДА ОТ ПРЕПОДАВАТЕЛЯ (не законченный)

/* int[] data = { 1, 2, 3, 413, 145, 2, 3, 4, };
string s = String.Empty;

int[,] storage = new int[data.Length, 2];
int count = 0;
for (int i = 0; i < data.Length; i++)
{
    for (int j = 0; j < data.Length; j++)
    {
        if (i != j)
        {
            if (data[i] == data[j]) count++;
        }
    }
} */



//================================================================================

/* Console.Clear очистка консоли

Console.Set.CursorPosition              Команда, которая будет «рисовать»

new Random().Next()                     генератор псевдослучайных чисел
Int numberA = new Random().Next(1, 10)   ввести число случайное от 1 до 10

Console.Write( для печати чего-либо в консоль )

Console.ReadLine(необходимая для того, чтобы что - то считать) */


/* /*  Console.WriteLine("Введите любое число: ");
int num = Convert.ToInt32(Console.ReadLine());      если надо ввести с клавиатуры число/цифру

int[]array = {11,21,31,41,15,61,17,18,19};     массив

Console.WriteLine(array[i]);     вызвать определенный элемент стоящий на i позиции

break                           «прервать», если нашли что нужно и больше не надо выполнять дальше алгоритм

if(array[index] == 589)
{
    Console.WriteLine(index); 
    break;
}


 new int[10]                   дословно означает «создай новый массив, где будет 10 элементов»
int[]array = new int[10];


int n = array.Length         n здесь указывает какая длина будет массива


Console.WriteLine($"{i} * {j} = {i*j}")     запись сразу в строку результата умножения */